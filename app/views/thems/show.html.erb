<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @them.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= @them.content %>
</p>

<%= link_to 'Edit', edit_them_path(@them) %> |
<%= link_to 'Back', thems_path %>
